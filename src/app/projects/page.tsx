// /projects URL — renders the same SPA root as `/`. The NavigationProvider
// reads `window.location.pathname` on first render and shows the Projects page
// directly (no hero flash on initial paint).
export { default } from '../page';
